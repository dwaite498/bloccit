<h1>Edit Sponsored Posts</h1>
<div class="row">
    <div class="col-md-4">
        <p>Guidelines for sponsored posts</p>
        <ul>
            <li>Make sure it rhymes</li>
            <li>Don't use the letter 'A'</li>
            <li>The use of hashtags AT ALL will result in a drone strike. . . on you.</li>
            <li>Remember, no hashtags.</li>
        </ul>
    </div>
    <div class="col-md-8">
     <%= form_for [@sponsored_post.topic, @sponsored_post] do |f| %>
               <div class="form-group">
                  <%= f.label :title %>
                  <%= f.text_field :title, class: 'form-control', placeholder: "Enter post title" %>
                 </div>
                 <div class="form-group">
                   <%= f.label :body %>
                   <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Type here is you dare" %>
                  </div>
                    <div class="form-group">
                   <%= f.label :price %>
                   <%= number_field(:sponsored_post, :price, in: 1.0..20.0, step: 0.5) %>
                  </div>
                  <div class="form-group">
                   <%= f.submit "Save", class: 'btn btn-success' %>
            </div>
        <% end %>
    </div>
</div>